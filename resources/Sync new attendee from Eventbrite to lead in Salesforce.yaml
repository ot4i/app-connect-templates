$integration: http://ibm.com/appconnect/integration/v2/integrationFile
integration:
  type: trigger-action
  trigger-interfaces:
    trigger-interface-1:
      connector-type: eventbrite
      type: event-trigger
      triggers:
        CREATED:
          input-context:
            data: Attendee
          assembly:
            $ref: '#/integration/assemblies/assembly-1'
          options:
            organizationID: '694036855583'
            parentFilter:
              organizationID: '694036855583'
  action-interfaces:
    action-interface-1:
      type: api-action
      business-object: Lead
      connector-type: salesforce
      actions:
        CREATE: {}
  assemblies:
    assembly-1:
      assembly:
        execute:
          - create-action:
              name: Salesforce Create lead
              target:
                $ref: '#/integration/action-interfaces/action-interface-1'
              map:
                mappings:
                  - Company:
                      template: '{{$Trigger.profile.company}}'
                  - Country:
                      template: '{{$Trigger.profile.addresses.home.country}}'
                  - Email:
                      template: '{{$Trigger.profile.email}}'
                  - FirstName:
                      template: '{{$Trigger.profile.first_name}}'
                  - LastName:
                      template: '{{$Trigger.profile.last_name}}'
                  - Website:
                      template: '{{$Trigger.profile.website}}'
                $map: http://ibm.com/appconnect/map/v1
                input:
                  - variable: Trigger
                    $ref: '#/trigger/payload'
  name: Sync new attendee from Eventbrite to lead in Salesforce
models: {}
