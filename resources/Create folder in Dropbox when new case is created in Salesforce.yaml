$integration: http://ibm.com/appconnect/integration/v2/integrationFile
integration:
  type: trigger-action
  trigger-interfaces:
    trigger-interface-1:
      type: event-trigger
      triggers:
        CREATED:
          input-context:
            data: Case
          assembly:
            $ref: '#/integration/assemblies/assembly-1'
      connector-type: salesforce
  action-interfaces:
    action-interface-1:
      type: api-action
      business-object: folders
      actions:
        CREATE: {}
      connector-type: dropbox
  assemblies:
    assembly-1:
      assembly:
        execute:
          - create-action:
              name: Dropbox Create folder
              target:
                $ref: '#/integration/action-interfaces/action-interface-1'
        tags:
          - incomplete
  name: Create folder in Dropbox when new case is created in Salesforce
models: {}
